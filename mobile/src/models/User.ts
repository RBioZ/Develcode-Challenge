export default interface IUser {
  id: string;
  name: string;
  code: string;
  avatar: string | null;
  birth_date: string;
  created_at: string;
  updated_at: string;
  // eslint-disable-next-line semi
}
